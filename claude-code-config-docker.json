{
  "_comment": "Docker variant. Copy the 'bookworm' entry into ~/.mcp.json (global) or <project>/.mcp.json (project). Adjust the docker-compose.yml path for the target machine.",
  "mcpServers": {
    "bookworm": {
      "command": "docker",
      "args": ["compose", "-f", "/CHANGE_ME/to/BookWorm/docker-compose.yml", "run", "--rm", "-T", "bookworm"],
      "env": {
        "OPENAI_API_KEY": ""
      }
    }
  }
}
