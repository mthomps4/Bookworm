{
  "_comment": "Docker variant. Copy the 'library-mcp' entry into ~/.claude/settings.json (global) or .claude/settings.json (project). Adjust the docker-compose.yml path for the target machine.",
  "mcpServers": {
    "library-mcp": {
      "command": "docker",
      "args": ["compose", "-f", "/CHANGE_ME/to/BookWorm/docker-compose.yml", "run", "--rm", "-T", "library-mcp"],
      "env": {
        "OPENAI_API_KEY": ""
      }
    }
  }
}
