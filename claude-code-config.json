{
  "_comment": "Copy the 'bookworm' entry into ~/.mcp.json (global) or <project>/.mcp.json (project). Adjust 'cwd' to this repo's absolute path on the target machine.",
  "mcpServers": {
    "bookworm": {
      "command": "python",
      "args": ["-m", "library_mcp.server"],
      "cwd": "/CHANGE_ME/to/BookWorm",
      "env": {
        "PYTHONPATH": "/CHANGE_ME/to/BookWorm/src",
        "OPENAI_API_KEY": ""
      }
    }
  }
}
