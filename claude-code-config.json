{
  "_comment": "Copy the 'library-mcp' entry into ~/.claude/settings.json (global) or .claude/settings.json (project). Adjust 'cwd' to this repo's absolute path on the target machine.",
  "mcpServers": {
    "library-mcp": {
      "command": "python",
      "args": ["-m", "library_mcp.server"],
      "cwd": "/CHANGE_ME/to/BookWorm",
      "env": {
        "PYTHONPATH": "/CHANGE_ME/to/BookWorm/src",
        "OPENAI_API_KEY": ""
      }
    }
  }
}
